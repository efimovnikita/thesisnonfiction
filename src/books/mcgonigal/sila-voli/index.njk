---
layout: posts.njk
eleventyNavigation:
  key: Mcgonigal
  ulr: /mcgonigal/sila-voli
  title: "Келли Макгонигал. Сила воли: Как развить и укрепить"
---

<section>
    <h2>Posts section</h2>
    {%- for post in collections.silaVoli -%}
    <article>
        <h2><a href="{{post.url}}">{{ post.data.title }}</a></h2>
        {{ post.templateContent | safe }}
        <footer>
        <img src="/images/calendar.svg" alt="date" />
        <time>{{ post.data.date }}</time>
        <img src="/images/folder.svg" alt="category" />
        {% set navPages = collections.all | eleventyNavigation %}
        {%- for entry in navPages %}
        {% if entry.title == post.data.book %}
            <aside><a href="{{ entry.url }}">{{ post.data.book }}</a></aside>
        {% endif %}
        {%- endfor %}
        </footer>
    </article>
    {%- endfor -%}
</section>