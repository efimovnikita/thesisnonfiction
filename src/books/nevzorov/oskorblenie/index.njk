---
layout: posts.njk
eleventyNavigation:
  key: Nevzorov
  ulr: /nevzorov/oskorblenie
  title: "Александр Невзоров. Искусство оскорблять"
---

<section>
    <h2>Posts section</h2>
    {%- for post in collections.oskorblenie -%}
    <article>
        <h2><a href="{{post.url}}">{{ post.data.title }}</a></h2>
        {{ post.templateContent | safe }}
        <footer>
        <img src="/images/calendar.svg" alt="date" />
        <time>{{ post.data.date }}</time>
        <img src="/images/folder.svg" alt="category" />
        {% set navPages = collections.all | eleventyNavigation %}
        {%- for entry in navPages %}
        {% if entry.title == post.data.book %}
            <aside><a href="{{ entry.url }}">{{ post.data.book }}</a></aside>
        {% endif %}
        {%- endfor %}
        </footer>
    </article>
    {%- endfor -%}
</section>