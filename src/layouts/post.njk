---
style: "post"
---

<!DOCTYPE html>
<html lang="ru">
  {% include "head.njk" %}
  <body>
    {% include "header.njk" %}
    <main>
      <section>
        <h2>Post section</h2>
        <article>
          <h2><a href="#">{{ title }}</a></h2>
          {{ content | safe }}
          <footer>
            <img src="/images/calendar.svg" alt="date" />
            <time>{{ date.toLocaleString('ru-RU', {year: "numeric",month: "2-digit",day: "numeric"}) }}</time>
            <img src="/images/folder.svg" alt="category" />
            {% set navPages = collections.all | eleventyNavigation %}
            {%- for entry in navPages %}
            {% if entry.title == book %}
             <aside><a href="{{ entry.url }}">{{ book }}</a></aside>
            {% endif %}
            {%- endfor %}
          </footer>
        </article>
      </section>
    </main>
    {% include "footer.njk" %}
    <script src="/scripts/script.js"></script>
  </body>
</html>
